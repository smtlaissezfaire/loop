(define-macro
  (if arg body ...)
  (cond (arg body ...)))

(define-macro
  (function body ...)
  (lambda body ...))

(define-macro
  (let ((key value) ...)
    body ...)
 ((lambda (key ...)
   body ...) value ...))
